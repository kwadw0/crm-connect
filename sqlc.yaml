version: "2"
sql:
  - engine: "postgresql"
    queries: "/internal/postgres/sqlc/queries.sql"
    schema: "/internal/postgres/migrations"
    gen:
      go:
        package: "repo"
        out: "/internal/postgres/repo"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
        overrides:
          - db_type: uuid
            go_type: github.com/google/uuid.UUID